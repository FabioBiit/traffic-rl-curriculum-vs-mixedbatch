% ============================================================
% Chapter 6 — Discussion (target 4–5 pp)
% ============================================================
\chapter{Discussion}
\label{ch:discussion}

This chapter interprets the findings of \cref{ch:results}: what task
ordering changed and what it did not (\cref{sec:ordering}), what the
shared-world frame contributed (\cref{sec:ptc-revisited}), and the
threats and scope limits under which every claim holds
(\cref{sec:threats}, \cref{sec:scope}).

% --- §6.1–6.2 drafted 2026-07-16 (full-matrix interpretation). ---
\section{What Task Ordering Changes (and What It Does Not)}
\label{sec:ordering}

Reading the four comparison axes of \cref{ch:results} together, task
ordering did something different from what the curriculum literature
usually promises and the mixed-training intuition usually fears.

\paragraph{Not learning speed.}
The axis where curricula are classically expected to pay --- early
sample efficiency --- showed nothing: all four cells crossed the same
windowed-SR thresholds within a few percent of the same step count
(\cref{sec:sample-efficiency}). A plausible reading is that this
platform's reward design already solves the problem curricula are
usually hired for: with dense per-waypoint bonuses and distance shaping
(\cref{sec:rewards}), even \SI{100}{m} routes provide learning signal
from the first episodes, so easy tasks are not needed to bootstrap.
Curricula built for sparse-reward exploration should not be expected to
show their signature advantage where the reward is dense --- a scope
condition on H1 rather than a refutation of curriculum learning at
large.

\paragraph{Equilibrium selection.}
What ordering did change is \emph{which policy the same learner became}.
The mechanics are visible in the constraints: completing a \SI{100}{m}
route within the 1{,}000-step horizon requires a healthy average speed,
so a policy exposed to hard routes from step~0 must drive fast to
succeed at all --- and pays in collisions (26\% at evaluation for
MLP-batch). A policy that spends its first quarter of training on
\SI{30}{m} routes can succeed slowly, and locks in caution (10.2~km/h,
failures dominated by stalling). Task ordering, in this reading, acts as
an \emph{equilibrium selector} over the speed--safety trade-off rather
than as an accelerator. The GNN cells support the selection reading from
the other side: with a different value estimator, the same curriculum
selected a different (faster) equilibrium
(\cref{sec:defensive-case}) --- the regime biases the choice of basin,
but does not determine it alone.

\paragraph{Not a shield against degradation.}
The within-run erosion of \cref{tab:quartiles} is platform-wide: it
affects both regimes, at fixed task level, in both classes. Whatever its
mechanism --- cross-class co-adaptation and entropy annealing being the
candidates on record --- ordering neither causes nor prevents it. The
practical implication for anyone reusing this platform is that
\emph{final} cumulative numbers understate mid-training peaks in every
configuration, and checkpoint selection during training would change
conclusions --- a reporting choice this thesis deliberately avoided in
favour of fixed-budget endpoints.

\paragraph{Generalization: the portable benefit.}
The one curriculum advantage that replicated across both critic
architectures is the Town05 margin ($+19.0$ and $+10.0$\,pp,
\cref{tab:interaction}) --- obtained despite batch's broader in-domain
coverage, and against H2's coverage-implies-robustness intuition. A
tentative mechanism consistent with both pairs: the batch policy
co-adapts for the whole budget to a single stationary distribution
(one map, all lengths), while the curriculum policy must stay competent
across a \emph{shifting} sequence of distributions --- a mild form of
implicit regularization through non-stationarity. At $n{=}1$ per cell
this remains a hypothesis; what the data establish is the direction and
its replication across architectures.

\paragraph{And the pedestrians.}
For the weaker, structurally constrained class
(\cref{sec:ped-limits}), ordering was irrelevant in three cells out of
four; the exception (GNN-curriculum, $+12$\,pp at evaluation) suggests
that pedestrians only benefit when the critic can represent cross-agent
structure \emph{and} exposure is staged. As a design lesson: regime
choices tuned on the strong class should not be assumed to transfer to
the weak one.

% ------------------------------------------------------------
\section{Parallel Task Coverage Revisited}
\label{sec:ptc-revisited}

The shared-world frame (\cref{sec:parallel-coverage}) was adopted for
throughput --- six task instances per episode --- with correlated
samples and mutual non-stationarity accepted as costs. In hindsight, the
campaign shows those "costs" doing first-order scientific work.

\paragraph{Interactions were load-bearing.}
The defensive equilibrium --- the single most consequential behavioural
phenomenon in the development record and in the MLP-curriculum cell ---
exists \emph{only} because vehicle rewards are gated on a hazard channel
fed by pedestrian behaviour (\cref{sec:rewards}). The mirrored
architecture finding points the same way: replacing a flat critic with
one that models agents as interacting nodes changed outcomes in every
cell (\cref{sec:arch-axis}), which it could not have done if
cross-agent structure were negligible. In a single-agent-per-world
design, none of this would have been visible.

\paragraph{Correlation is real and was treated as such.}
Six records per episode share one world state; treating them as
independent would overstate evidence. The thesis therefore never leans
on record counts for inference (\cref{sec:threats}), and the
evaluation-pipeline failure of \cref{sec:bugs} --- where collapsing
world seeds made all episodes near-identical and produced a flat,
too-clean success rate --- is the cautionary limit case of the same
correlation pushed to degeneracy.

\paragraph{Non-stationarity has a visible signature.}
Each agent trains against five moving co-players. The level-controlled
within-run erosion of \cref{tab:quartiles}, strongest exactly where
value estimation is weakest (MLP cells) and halved under the
relational critic, is the pattern co-adaptation drift would produce ---
though the design cannot isolate it from other late-training effects
(\cref{sec:threats}).

\paragraph{What the frame cannot claim.}
Without a one-agent-per-world control (\cref{sec:scope}), the
throughput benefit of parallel task coverage is a design argument, not a
measured one; and every finding above is conditional on this
interaction-rich regime. The frame should be read as the environment
\emph{within which} the regime comparison holds, not as an
independently validated technique.

% --- §6.3–6.4 drafted 2026-07-14 (results-independent; re-read after
%     Ch. 5 is final to update the n-per-cell statement). ---
\section{Threats to Validity}
\label{sec:threats}

\paragraph{Simulator non-determinism under identical conditions.}
The dominant threat, quantified rather than assumed: three full-length
runs of byte-identical code, configuration and seed produced cumulative
vehicle success rates ranging 52.2--62.4\% (\cref{sec:iterations}).
CARLA under multi-agent load is not run-to-run deterministic even in
synchronous mode with all seeds fixed. Every effect in this thesis is
therefore read against this $\sim$10\,pp band: differences within it are
never attributed to an experimental factor, whatever their direction. The
band was measured on the pre-campaign trunk at training level; it is
assumed representative for campaign-era runs, which is itself an
approximation.

\paragraph{Sample size.}
Each cell contains a single run: the design was reduced from three paired
seeds per cell to one for compute reasons, a decision declared in
\cref{sec:campaign}. Every conclusion therefore rests on paired
contrasts, not on estimated distributions. Seed pairing and
byte-identical configurations make the contrasts internally clean, and
the primary contrast carries one built-in replication --- it is observed
independently under both critic architectures --- but no formal
statistical inference is possible at this $n$, and none is claimed. The
mitigations are the replicate band above and consistency checks ---
direction agreement across difficulty levels, evaluation scenarios,
agent classes, and measurement layers --- rather than significance
tests.

\paragraph{Evaluation noise.}
The final evaluation uses 100 episodes per scenario, i.e.\ 300 vehicle
records; at mid-range success rates the binomial standard error alone is
$\approx$3\,pp per scenario, before accounting for the within-episode
correlation of the three vehicles, which lowers the effective sample
further. Scenario-level differences of a few points are therefore not
individually meaningful; only patterns across scenarios are interpreted.

\paragraph{Reward--curriculum coupling.}
The reward function was tuned through gate iterations run mostly on
easy-locked configurations (\cref{sec:gate}), so the trunk's shaping may
be implicitly biased toward short routes. Both regimes share the
identical reward, so the primary contrast remains internally valid; the
caveat applies to interpreting \emph{absolute} performance on hard
routes, and to the possibility that a differently tuned reward would
shift the two regimes differently.

\paragraph{Single map pair.}
Generalization is measured on exactly one transfer, Town03 $\to$ Town05.
The two layouts differ in kind (\cref{sec:carla-bg}), which makes the
test meaningful, but claims are limited to this pair; no evidence is
offered that the ranking of regimes transfers to other maps, traffic
compositions, or weather conditions.

\paragraph{Measurement history.}
The evaluation pipeline itself failed validity once
(\cref{sec:bugs}), and the route-length defect silently redefined the
task distribution for a whole era of experiments. Both were caught by
integrity signals (implausibly regular outputs; missing per-agent logs).
The protocol now in place --- integrity checks, paired configurations,
recomputation from raw logs --- was built in response, but the general
lesson stands as a threat class: in a custom platform, apparent effects
must first survive the question ``is the instrument measuring what it
claims?''

\paragraph{Pedestrian route feasibility.}
Pedestrian results at medium/hard difficulty are confounded by a
structural environment limit: Town03's sidewalk topology often cannot
support the nominal route targets (\cref{sec:routes}). \Cref{sec:ped-limits}
separates environment-attributable from policy-attributable failure, and
pedestrian-side regime conclusions are restricted accordingly.

% ------------------------------------------------------------
\section{Scope Limitations}
\label{sec:scope}

Beyond threats to internal validity, the design deliberately fixes
several dimensions; findings should not be read beyond them.

\begin{itemize}
  \item \textbf{Actor architecture is fixed.} Both classes use MLP
        actors throughout; only the \emph{critic encoder} varies (MLP
        vs.\ GNN, training-side only). This thesis makes no claim about
        policy-network architecture, and the GNN axis speaks only to
        value estimation under CTDE.
  \item \textbf{The shared-world frame is not ablated.} Parallel task
        coverage (\cref{sec:parallel-coverage}) is the fixed frame of
        both arms, not a treatment: there is no 1-agent-per-world
        control, so its causal contribution to sample efficiency is
        characterized, not isolated.
  \item \textbf{Privileged observations, no perception.} Agents observe
        curated state features (\cref{sec:obs-actions}), not sensors.
        All findings concern decision-making under interaction;
        nothing follows for camera- or lidar-based pipelines.
  \item \textbf{One algorithm, one platform version.} MAPPO on
        RLlib~2.10.0 and CARLA~0.9.16 throughout. Regime effects are
        conditional on this stack; other algorithms may interact with
        task ordering differently.
  \item \textbf{Difficulty is route length only.} The configuration
        defines traffic-density and mixed difficulty families, but they
        are unused (\cref{sec:campaign}); ``difficulty'' in every claim
        means target route length at fixed traffic.
  \item \textbf{Fixed population.} Always 3 vehicles $+$ 3 pedestrians;
        no scaling evidence in either direction.
  \item \textbf{Pedestrian route generation.} The sidewalk-chain planner
        with its crossing cap is part of the platform being studied; its
        known feasibility limits bound what pedestrian policies can
        express, independently of training regime.
\end{itemize}
